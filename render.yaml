services:
  - type: web
    name: email-assistance-backend
    runtime: docker
    buildCommand: "./mvnw clean package -DskipTests"
    startCommand: "java -jar target/*.jar"
    plan: free
    envVars:
      - key: PORT
        value: 8080
